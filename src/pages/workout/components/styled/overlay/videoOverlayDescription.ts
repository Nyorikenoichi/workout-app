import { Typography } from '@mui/material';
import styled from 'styled-components';

export const VideoOverlayDescription = styled(Typography)`
  margin: 8px 0 16px;
  padding: 0px 5px;
  font-weight: 400;
  font-size: 16px;
  opacity: 1;
`;
