import { Typography } from '@mui/material';
import styled from 'styled-components';

export const ExercisesTitle = styled(Typography)`
  margin-bottom: 10px;
  font-weight: 600;
  font-size: 24px;
`;
